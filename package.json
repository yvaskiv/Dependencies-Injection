{
  "name": "di-task",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "clean": "rm -rf ./dist && mkdir dist",
    "build:server": "tsc --build tsconfig.es5.json",
    "build:client": "tsc-watch --build tsconfig.es6.json --onSuccess \"node dist/server.js\"",
    "start": "yarn clean && yarn build:server && yarn build:client"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.1"
  },
  "devDependencies": {
    "@types/express": "^4.17.13",
    "@types/node": "^18.0.5",
    "@types/whatwg-fetch": "^0.0.33",
    "tsc-watch": "^5.0.3",
    "typescript": "^4.7.4"
  }
}
